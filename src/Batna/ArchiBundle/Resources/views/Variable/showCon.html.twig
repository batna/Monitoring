{% extends 'BatnaArchiBundle:Contexte:index.html.twig' %}

{% block variables %}

<div class="grid_12">
	<div class="block-border">
		<div class="block-header">
			<h1>{{ 'var.consultVar'|trans([], 'BatnaArchiBundle') }}</h1><span></span>
		</div>
		<div class="block-content">
			<table style="margin:10px;">
			    <tbody>
			        <tr>
			            <th>{{ 'var.name'|trans({}, 'BatnaArchiBundle') }} : </th>
			            <td>{{ entity.name }}</td>
			        </tr>
			    </tbody>
			</table>
			<div class="block-actions">
				<ul class="actions-left">
				    <li>
				        <a href="{{ path('ctxt') }}" class="button">{{ 'context.backToTheList'|trans({}, 'BatnaArchiBundle') }}</a>
				    </li>
				</ul>
				<ul class="actions-right">
					<li>
						<a href="{{ path('varCon_edit', { 'id': entity.id }) }}" class="button">{{ 'context.edit'|trans({}, 'BatnaArchiBundle') }}</a>
					</li>
				</form>
					<li>
						<form action="{{ path('varCon_delete', { 'id': entity.id }) }}" method="post">
				            {{ form_widget(delete_form) }}
            				<input type="submit" class="button red" value="{{ 'context.delete'|trans({}, 'BatnaArchiBundle') }}" />
        				</form>
        			</li>
				</ul>
			</div>
		</div>
	</div>
</div>

{% endblock %}